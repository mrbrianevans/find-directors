{
  "name": "src",
  "version": "1.0.0",
  "author": "Brian Evans",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build:types": "tsc --build",
    "build:functions": "node --experimental-import-meta-resolve build.js",
    "build": "tsc --build && node --experimental-import-meta-resolve build.js",
    "watch:types": "tsc --build --watch",
    "clean": "tsc --build --clean",
    "test": "node -r dotenv/config --test "
  },
  "devDependencies": {
    "@companieshouse/api-sdk-node": "^2.0.35",
    "@types/node": "^18.0.0",
    "dotenv": "^16.0.2",
    "esbuild": "^0.15.9",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "jsonschema": "^1.4.1"
  }
}
